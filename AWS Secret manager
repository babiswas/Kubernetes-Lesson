
A secure place to store secret items like username and password.
Application secrets can be stored in aws secret maneger.
Application pulls secret from secret manager.
Application should have secret manager role to access the secret.
Cloudtrail is used to monitor who has acessed the secrets.

AWS Secret management
=======================
Docker(username,password,registry url)
Database(username,password)
Teraaform(aws provider credentials)

1.System manager 
2.Secrets manager
3.Hashicorp Vault

System manager
a.Parameter Store

Why use other services if system manager is there?

AWS introduced secret manager because it performs secret rotation.
Other services is provided IAM role to interact with system manager.
Systems manager can rotate certificate and password

Use secret manager when we use Databases,API tokens
Less sensitive item go for system manager
More sensitive information go for secret managet



