The scope of VPC is region(USA ,Europe,Mumbai)
The scope of subnet(Datacenter) is availibility zone within a region.
1.Internet Gateway(region)
2.Routing Table(region)
3.Security Group(region)
VPC endpoint:To communicate with AWS resource if we have to route traffic through AWS use VPC endpoint.
Attach internet gateway to VPC
Attach route table with VPC

A subnet associated with Internet gateway is the public subnet.
Associate the route table with subnet.

To access ec2 instance in public subnet the privatekey should be there in the local from where acess is required.

VPC endpoint
==============
Associate VPC endpoint with route table.
Associate AWS service.
Associate VPC




